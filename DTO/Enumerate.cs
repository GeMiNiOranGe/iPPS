namespace DTO {
    public enum EmployeeType {
        InternOrTrainee,
        PartTime,
        FullTime,
    }

    public enum AccessLevel {
        CEO,
        Manager,
        Employee,
    }

    public enum StatusType {
        OnGoing,
        Pause,
        Cancel,
    }
}
